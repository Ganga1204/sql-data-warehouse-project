name: 'dbt_warehouse'
version: '1.0.0'
config-version: 2

profile: 'dbt_warehouse'

model-paths: ["models"]
test-paths: ["tests"]

models:
  dbt_warehouse:
    staging:
      schema: staging
      materialized: view
    intermediate:
      schema: intermediate
      materialized: view
    marts:
      schema: marts
      materialized: table